# 作品集网站加载速度优化说明

## 已在项目里做的改动

1. **首张主图预加载**（`index.html`）  
   浏览器会优先请求排序后的第一张照片压缩图，大图出现更快。  
   **注意：** 若你改了 `data.js` 里排序规则或首张作品，请把 `<link rel="preload" … href="…">` 改成与**排序后第一项**相同的压缩图路径。

2. **缩略图加载策略**（`js/main.js`）  
   列表按距当前项由近到远分批加载，避免一打开就并发下载几十张图。  
   列表先用 `photos/摄影作品压缩/` 压缩图，主图再在叠层加载原图。

3. **主图**  
   压缩图垫底 + 原图叠层，避免切换时空窗白闪。

4. **CDN 缓存**（腾讯云 CDN 控制台）  
   `/photos/` 长期缓存（365 天），`/js/`、`/css/`、`index.html` 短缓存或不缓存。配置见 [《部署到腾讯云COS+CDN.md》](部署到腾讯云COS+CDN.md)。

---

## 为什么还是会慢？

部分「压缩图」分辨率仍接近原图（3000px+、数百 KB），主图原图平均约 **1.8MB**。切换作品时仍要下载大图。

**最有效的进一步优化（需本地处理图片）：**

| 做法 | 效果 |
|------|------|
| **真正的小图** | 列表长边 **400px**、质量 75%，目标 **30–80KB** | 首屏流量可再降一个数量级 |
| **压缩主图** | 展示用长边 **1600–2400px** | 主图加载明显变快 |
| **WebP** | 同目录提供 `.webp` | 体积再降约 25～35% |

---

## 国内访问

站点托管在 **腾讯云香港 COS + 境外 CDN**。深圳等华南用户通常比原 Vercel（新加坡）快 **2～3 倍**，**无需 VPN**。

若完成 **ICP 备案**，可切换 **境内 CDN + 大陆 COS**，国内速度还可再提升数倍。

---

## 可选：字体

当前使用 [fonts.loli.net](https://fonts.loli.net) 镜像 Google Fonts。若仍慢，可把 **DM Sans** 自托管到 `fonts/`，或改用系统字体栈（改 `css/style.css` 里 `font-family`）。
