# 购买个人域名并绑定到网站

`yi-photo.cn` 已在 **腾讯云** 购买。站点托管在 **腾讯云香港 COS + 境外 CDN**（见 [《部署到腾讯云COS+CDN.md》](部署到腾讯云COS+CDN.md)），不再使用 Vercel。

---

## 一、在哪里买域名

| 类型     | 举例                     | 说明 |
|----------|--------------------------|------|
| **国内** | 阿里云（万网）、腾讯云、华为云 | 用身份证即可注册，价格约 50–80 元/年（.cn / .com） |
| **国外** | Namecheap、Cloudflare     | 用信用卡，价格约 $10–15/年 |

**yi-photo.cn** 在腾讯云注册，DNS 解析也在 [腾讯云 DNS 控制台](https://console.cloud.tencent.com/cns) 管理。

---

## 二、绑定到腾讯云 CDN（当前方案）

1. **在 CDN 控制台添加加速域名**
   - 打开 [CDN 控制台](https://console.cloud.tencent.com/cdn)
   - **加速区域**：境外（含港澳台）— 不备案场景
   - 添加 `yi-photo.cn`（以控制台校验为准）或使用 CDN 默认域名

2. **在腾讯云 DNS 做解析**
   - 删除原指向 Vercel 的 A 记录（`@ → 216.198.79.1`）
   - 新增 **CNAME**：主机记录 `@` → CDN 提供的 CNAME 地址
   - HTTPS 在 CDN 控制台开启（免费证书）

3. **等生效**
   - 解析一般 10 分钟～几小时生效

---

## 三、备案说明

| 场景 | 是否需要备案 |
|------|-------------|
| CDN **境外加速** + 未备案 | 可尝试绑 `.cn`，以控制台为准；**不需要 VPN** |
| CDN **中国境内加速** + `.cn` | **必须备案** |
| 备案后追求深圳/国内最快速度 | 建议切换 **境内 CDN** + **大陆 COS 桶**（广州） |

备案入口：腾讯云控制台 → **网站备案**。约 1～2 周。

---

## 四、从 Vercel 迁移

若 `yi-photo.cn` 仍在 Vercel：

1. Vercel → 项目 → **Domains** → 删除 `yi-photo.cn`
2. 按上文将 DNS 改指腾讯云 CDN
3. 可选：删除 Vercel 项目

---

**总结：** 域名在腾讯云，站点在香港 COS + 境外 CDN；深圳用户无需 VPN，通常比 Vercel 快。长期面向国内可考虑备案并升级境内 CDN。
